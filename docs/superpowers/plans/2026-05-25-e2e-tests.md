# E2E Tests Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 Forge 前端补全 Playwright E2E 测试，覆盖所有已实现功能，供 agent 自驱动验收使用。

**Architecture:** Playwright 配置在项目根目录，测试分两层：Layer 1（无 SSE，稳定）覆盖路由/登录/项目列表/WorkspacePage 基础交互；Layer 2（SSE mock）覆盖 Agent 进度流/预览面板，用 `page.route()` 拦截 SSE 端点注入假事件流。每个测试文件对应一个功能域，共享 `fixtures/auth.ts` 处理登录状态。

**Tech Stack:** Playwright 1.x，TypeScript，Vite dev server（`localhost:5173`），Go API（`localhost:8080`）

---

## 前置条件（运行测试前必须满足）

- Go API 在 `localhost:8080` 运行（`cd apps/api && go run ./cmd/server`）
- Vite dev server 在 `localhost:5173` 运行（`cd apps/web && pnpm dev`）
- dev 账号 `dev@forge.local` / `devpassword123` 已注册（第一次跑 `skip login` 会自动注册）

---

## File Map

| 操作 | 文件 | 职责 |
|------|------|------|
| 新建 | `playwright.config.ts` | Playwright 根配置 |
| 新建 | `e2e/fixtures/auth.ts` | 登录 fixture，注入真实 JWT |
| 新建 | `e2e/layer1/routing.spec.ts` | 路由重定向 + 守卫 |
| 新建 | `e2e/layer1/login.spec.ts` | 登录流程（skip 按钮） |
| 新建 | `e2e/layer1/projects.spec.ts` | 项目列表（空状态 + 有项目） |
| 新建 | `e2e/layer1/workspace-input.spec.ts` | WorkspacePage input 阶段 |
| 新建 | `e2e/layer1/workspace-pm-review.spec.ts` | PMReview 阶段功能选择 |
| 新建 | `e2e/layer1/workspace-running.spec.ts` | running 阶段 UI（mock projectId） |
| 新建 | `e2e/layer2/agent-flow.spec.ts` | Agent 进度卡片（SSE mock） |
| 新建 | `e2e/layer2/preview-panel.spec.ts` | 预览面板 iframe（SSE mock） |
| 新建 | `e2e/layer2/waiting-state.spec.ts` | waiting 阶段用户输入（SSE mock） |
| 修改 | `apps/web/package.json` | 不改，Playwright 装在根目录 |
| 新建 | `package.json`（根） | 如不存在则新建，添加 e2e script |

---

### Task 1: 安装 Playwright，配置根目录

**Files:**
- Create: `playwright.config.ts`
- Modify: `package.json`（根目录）

- [ ] **Step 1: 检查根 package.json 是否存在**

```bash
ls /Users/cookie/project/forge/package.json
```

如果不存在，创建：
```json
{
  "name": "forge",
  "private": true,
  "scripts": {
    "e2e": "playwright test",
    "e2e:layer1": "playwright test e2e/layer1",
    "e2e:layer2": "playwright test e2e/layer2"
  }
}
```

如果存在，在 `scripts` 里添加：
```json
"e2e": "playwright test",
"e2e:layer1": "playwright test e2e/layer1",
"e2e:layer2": "playwright test e2e/layer2"
```

- [ ] **Step 2: 安装 Playwright**

```bash
cd /Users/cookie/project/forge && npm install -D @playwright/test
npx playwright install chromium
```

期望：`node_modules/@playwright/test` 存在，chromium 下载完成。

- [ ] **Step 3: 创建 playwright.config.ts**

```typescript
import { defineConfig, devices } from '@playwright/test'

export default defineConfig({
  testDir: './e2e',
  fullyParallel: false,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  workers: 1,
  reporter: [['list'], ['html', { open: 'never' }]],
  use: {
    baseURL: 'http://localhost:5173',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
  // Do NOT use webServer — frontend and API are started manually
})
```

- [ ] **Step 4: 创建 e2e 目录结构**

```bash
mkdir -p /Users/cookie/project/forge/e2e/fixtures
mkdir -p /Users/cookie/project/forge/e2e/layer1
mkdir -p /Users/cookie/project/forge/e2e/layer2
```

- [ ] **Step 5: 验证 Playwright 能找到配置**

```bash
cd /Users/cookie/project/forge && npx playwright test --list 2>&1 | head -5
```

期望：输出 `No tests found` 或列出测试（此时还没有 spec 文件）。

- [ ] **Step 6: Commit**

```bash
cd /Users/cookie/project/forge
git add playwright.config.ts package.json e2e/
git commit -m "chore: add Playwright config and e2e directory structure"
```

---

### Task 2: Auth fixture

**Files:**
- Create: `e2e/fixtures/auth.ts`

- [ ] **Step 1: 创建 auth fixture**

```typescript
// e2e/fixtures/auth.ts
// Provides an authenticated page fixture.
// Calls the real API to register+login the dev account, injects the JWT
// into localStorage so the Zustand store picks it up via the normal flow.
// NOTE: Zustand store is in-memory — we inject token by performing the
// skip-login flow through the UI, not by patching localStorage directly.

import { test as base, type Page } from '@playwright/test'

export type AuthFixtures = {
  authedPage: Page
}

async function performDevLogin(page: Page) {
  await page.goto('/login')
  await page.getByRole('button', { name: '→ 快速登录（开发模式）' }).click()
  // Wait until redirected to /projects
  await page.waitForURL('**/projects', { timeout: 10_000 })
}

export const test = base.extend<AuthFixtures>({
  authedPage: async ({ page }, use) => {
    await performDevLogin(page)
    await use(page)
  },
})

export { expect } from '@playwright/test'
```

- [ ] **Step 2: 验证 fixture 文件 TypeScript 正确**

```bash
cd /Users/cookie/project/forge && npx tsc --noEmit --project tsconfig.json 2>&1 || echo "no root tsconfig, skip"
```

---

### Task 3: Layer 1 — 路由测试

**Files:**
- Create: `e2e/layer1/routing.spec.ts`

- [ ] **Step 1: 创建 routing.spec.ts**

```typescript
// e2e/layer1/routing.spec.ts
// Tests: route redirects and auth guard behavior

import { test, expect } from '@playwright/test'

test.describe('routing', () => {
  test('/ redirects to /projects', async ({ page }) => {
    await page.goto('/')
    await expect(page).toHaveURL(/\/login/)
    // / → /projects → /login (no token)
  })

  test('unauthenticated access to /projects redirects to /login', async ({ page }) => {
    await page.goto('/projects')
    await expect(page).toHaveURL(/\/login/)
  })

  test('unauthenticated access to /projects/new redirects to /login', async ({ page }) => {
    await page.goto('/projects/new')
    await expect(page).toHaveURL(/\/login/)
  })

  test('/login page renders brand name, login form and dev button', async ({ page }) => {
    await page.goto('/login')
    await expect(page.getByRole('heading', { name: 'Forge' })).toBeVisible()
    await expect(page.getByPlaceholder('邮箱')).toBeVisible()
    await expect(page.getByPlaceholder('密码')).toBeVisible()
    await expect(page.getByRole('button', { name: '登录' })).toBeVisible()
    await expect(page.getByRole('button', { name: /快速登录（开发模式）/ })).toBeVisible()
  })
})
```

- [ ] **Step 2: 运行测试（期望通过）**

```bash
cd /Users/cookie/project/forge && npx playwright test e2e/layer1/routing.spec.ts --reporter=line
```

期望：4 tests passed。

- [ ] **Step 3: Commit**

```bash
git add e2e/layer1/routing.spec.ts
git commit -m "test(e2e): routing redirects and auth guard"
```

---

### Task 4: Layer 1 — 登录测试

**Files:**
- Create: `e2e/layer1/login.spec.ts`

- [ ] **Step 1: 创建 login.spec.ts**

```typescript
// e2e/layer1/login.spec.ts
// Tests: skip-login flow via real API

import { test, expect } from '@playwright/test'

test.describe('login — dev mode', () => {
  test('dev login button calls register+login API and redirects to /projects', async ({ page }) => {
    await page.goto('/login')

    const registerRequest = page.waitForRequest(req =>
      req.url().includes('/api/v1/auth/register') && req.method() === 'POST'
    )
    const loginRequest = page.waitForRequest(req =>
      req.url().includes('/api/v1/auth/login') && req.method() === 'POST'
    )

    await page.getByRole('button', { name: /快速登录（开发模式）/ }).click()

    await registerRequest
    await loginRequest

    await page.waitForURL('**/projects', { timeout: 10_000 })
    await expect(page).toHaveURL(/\/projects$/)
  })

  test('dev login button shows loading state while in flight', async ({ page }) => {
    await page.goto('/login')

    await page.route('**/api/v1/auth/login', async route => {
      await new Promise(r => setTimeout(r, 300))
      await route.continue()
    })

    await page.getByRole('button', { name: /快速登录（开发模式）/ }).click()
    await expect(page.getByRole('button', { name: '登录中...' })).toBeVisible()
    await page.waitForURL('**/projects', { timeout: 10_000 })
  })
})

test.describe('login — real form', () => {
  test('email and password inputs are enabled and editable', async ({ page }) => {
    await page.goto('/login')
    const emailInput = page.getByPlaceholder('邮箱')
    const passwordInput = page.getByPlaceholder('密码')
    await expect(emailInput).toBeEnabled()
    await expect(passwordInput).toBeEnabled()
    await emailInput.fill('test@example.com')
    await expect(emailInput).toHaveValue('test@example.com')
  })

  test('shows error on wrong credentials', async ({ page }) => {
    await page.goto('/login')
    await page.getByPlaceholder('邮箱').fill('wrong@example.com')
    await page.getByPlaceholder('密码').fill('wrongpassword')
    await page.getByRole('button', { name: '登录' }).click()
    await expect(page.getByText('邮箱或密码错误')).toBeVisible({ timeout: 5_000 })
  })

  test('shows validation error when fields are empty', async ({ page }) => {
    await page.goto('/login')
    await page.getByRole('button', { name: '登录' }).click()
    await expect(page.getByText('请输入邮箱和密码')).toBeVisible()
  })

  test('real login works with dev credentials', async ({ page }) => {
    // Ensure dev account exists first
    await page.request.post('http://localhost:8080/api/v1/auth/register', {
      data: { email: 'dev@forge.local', password: 'devpassword123', name: 'Dev User' },
    }).catch(() => {}) // ignore 409

    await page.goto('/login')
    await page.getByPlaceholder('邮箱').fill('dev@forge.local')
    await page.getByPlaceholder('密码').fill('devpassword123')
    await page.getByRole('button', { name: '登录' }).click()
    await page.waitForURL('**/projects', { timeout: 10_000 })
    await expect(page).toHaveURL(/\/projects$/)
  })
})
```

- [ ] **Step 2: 运行测试**

```bash
cd /Users/cookie/project/forge && npx playwright test e2e/layer1/login.spec.ts --reporter=line
```

期望：3 tests passed。

- [ ] **Step 3: Commit**

```bash
git add e2e/layer1/login.spec.ts e2e/fixtures/auth.ts
git commit -m "test(e2e): login flow with skip button"
```

---

### Task 5: Layer 1 — 项目列表测试

**Files:**
- Create: `e2e/layer1/projects.spec.ts`

- [ ] **Step 1: 创建 projects.spec.ts**

```typescript
// e2e/layer1/projects.spec.ts
// Tests: ProjectsPage empty state, project cards, navigation

import { test, expect } from './fixtures'

// Re-export from fixtures
export { test, expect }

// Separate file needed — import from local fixtures
```

实际内容（完整文件，不用上面的占位）：

```typescript
// e2e/layer1/projects.spec.ts
import { test, expect } from '../fixtures/auth'

test.describe('projects page', () => {
  test.beforeEach(async ({ authedPage: page }) => {
    // Mock the projects API to return empty list for isolation
    await page.route('**/api/v1/projects', async route => {
      if (route.request().method() === 'GET') {
        await route.fulfill({
          status: 200,
          contentType: 'application/json',
          body: JSON.stringify({ data: [], total: 0, page: 1, limit: 20 }),
        })
      } else {
        await route.continue()
      }
    })
    await page.goto('/projects')
  })

  test('shows empty state when no projects', async ({ authedPage: page }) => {
    await expect(page.getByText('还没有项目')).toBeVisible()
    await expect(page.getByText('用自然语言描述你的 App，Agent 团队来生成它')).toBeVisible()
    await expect(page.getByRole('button', { name: '创建第一个项目' })).toBeVisible()
  })

  test('empty state CTA navigates to /projects/new', async ({ authedPage: page }) => {
    await page.getByRole('button', { name: '创建第一个项目' }).click()
    await expect(page).toHaveURL(/\/projects\/new/)
  })

  test('header shows 我的项目 title and + 新建项目 button', async ({ authedPage: page }) => {
    await expect(page.getByRole('heading', { name: '我的项目' })).toBeVisible()
    await expect(page.getByRole('button', { name: '+ 新建项目' })).toBeVisible()
  })

  test('+ 新建项目 button navigates to /projects/new', async ({ authedPage: page }) => {
    await page.getByRole('button', { name: '+ 新建项目' }).click()
    await expect(page).toHaveURL(/\/projects\/new/)
  })
})

test.describe('projects page — with projects', () => {
  test('shows project cards with correct status badges', async ({ authedPage: page }) => {
    // Seed mock projects covering all status variants
    await page.route('**/api/v1/projects', async route => {
      await route.fulfill({
        status: 200,
        contentType: 'application/json',
        body: JSON.stringify({
          data: [
            { id: 'p1', name: '报销系统', userId: 'u1', status: 'done', previewUrl: 'https://preview.e2b.dev/p1', createdAt: '2026-05-25T00:00:00Z', updatedAt: '2026-05-25T00:00:00Z' },
            { id: 'p2', name: '任务管理', userId: 'u1', status: 'building', previewUrl: '', createdAt: '2026-05-25T00:00:00Z', updatedAt: '2026-05-25T00:00:00Z' },
            { id: 'p3', name: '电商后台', userId: 'u1', status: 'failed', previewUrl: '', createdAt: '2026-05-25T00:00:00Z', updatedAt: '2026-05-25T00:00:00Z' },
          ],
          total: 3, page: 1, limit: 20,
        }),
      })
    })

    await page.goto('/projects')

    // Project names visible
    await expect(page.getByText('报销系统')).toBeVisible()
    await expect(page.getByText('任务管理')).toBeVisible()
    await expect(page.getByText('电商后台')).toBeVisible()

    // Status badges
    await expect(page.getByText('完成')).toBeVisible()
    await expect(page.getByText('生成中')).toBeVisible()
    await expect(page.getByText('失败')).toBeVisible()

    // Project count shown
    await expect(page.getByText('3 个项目')).toBeVisible()
  })

  test('done project shows 预览 and 打开 buttons', async ({ authedPage: page }) => {
    await page.route('**/api/v1/projects', async route => {
      await route.fulfill({
        status: 200,
        contentType: 'application/json',
        body: JSON.stringify({
          data: [{ id: 'p1', name: '报销系统', userId: 'u1', status: 'done', previewUrl: 'https://preview.e2b.dev/p1', createdAt: '2026-05-25T00:00:00Z', updatedAt: '2026-05-25T00:00:00Z' }],
          total: 1, page: 1, limit: 20,
        }),
      })
    })
    await page.goto('/projects')
    await expect(page.getByRole('button', { name: '预览' })).toBeVisible()
    await expect(page.getByRole('button', { name: '打开' })).toBeVisible()
  })

  test('building project shows 查看进度 button', async ({ authedPage: page }) => {
    await page.route('**/api/v1/projects', async route => {
      await route.fulfill({
        status: 200,
        contentType: 'application/json',
        body: JSON.stringify({
          data: [{ id: 'p2', name: '任务管理', userId: 'u1', status: 'building', previewUrl: '', createdAt: '2026-05-25T00:00:00Z', updatedAt: '2026-05-25T00:00:00Z' }],
          total: 1, page: 1, limit: 20,
        }),
      })
    })
    await page.goto('/projects')
    await expect(page.getByRole('button', { name: '查看进度' })).toBeVisible()
  })

  test('failed project shows 重试 and 删除 buttons', async ({ authedPage: page }) => {
    await page.route('**/api/v1/projects', async route => {
      await route.fulfill({
        status: 200,
        contentType: 'application/json',
        body: JSON.stringify({
          data: [{ id: 'p3', name: '电商后台', userId: 'u1', status: 'failed', previewUrl: '', createdAt: '2026-05-25T00:00:00Z', updatedAt: '2026-05-25T00:00:00Z' }],
          total: 1, page: 1, limit: 20,
        }),
      })
    })
    await page.goto('/projects')
    await expect(page.getByRole('button', { name: '重试' })).toBeVisible()
    await expect(page.getByRole('button', { name: '删除' })).toBeVisible()
  })

  test('clicking 打开 on a done project navigates to /projects/:id', async ({ authedPage: page }) => {
    await page.route('**/api/v1/projects', async route => {
      await route.fulfill({
        status: 200,
        contentType: 'application/json',
        body: JSON.stringify({
          data: [{ id: 'proj-123', name: '报销系统', userId: 'u1', status: 'done', previewUrl: 'https://preview.e2b.dev/p1', createdAt: '2026-05-25T00:00:00Z', updatedAt: '2026-05-25T00:00:00Z' }],
          total: 1, page: 1, limit: 20,
        }),
      })
    })
    await page.goto('/projects')
    await page.getByRole('button', { name: '打开' }).click()
    await expect(page).toHaveURL(/\/projects\/proj-123/)
  })
})
```

- [ ] **Step 2: 运行测试**

```bash
cd /Users/cookie/project/forge && npx playwright test e2e/layer1/projects.spec.ts --reporter=line
```

期望：9 tests passed。

- [ ] **Step 3: Commit**

```bash
git add e2e/layer1/projects.spec.ts
git commit -m "test(e2e): projects page empty state and project cards"
```

---

### Task 6: Layer 1 — WorkspacePage input 阶段

**Files:**
- Create: `e2e/layer1/workspace-input.spec.ts`

- [ ] **Step 1: 创建 workspace-input.spec.ts**

```typescript
// e2e/layer1/workspace-input.spec.ts
// Tests: WorkspacePage input phase UI and interactions

import { test, expect } from '../fixtures/auth'

test.describe('workspace input phase', () => {
  test.beforeEach(async ({ authedPage: page }) => {
    await page.goto('/projects/new')
  })

  test('shows three-column layout', async ({ authedPage: page }) => {
    // Left panel
    await expect(page.getByText('🔨 Forge')).toBeVisible()
    // Center panel
    await expect(page.getByText('Agent 协作流程')).toBeVisible()
    // Right panel — preview placeholder
    await expect(page.getByText('输入需求后预览将出现在这里')).toBeVisible()
  })

  test('shows requirement textarea and submit button', async ({ authedPage: page }) => {
    await expect(page.getByPlaceholder(/我需要|做一个|我想要|帮我做/)).toBeVisible()
    await expect(page.getByRole('button', { name: /生成应用/ })).toBeVisible()
  })

  test('submit button is disabled when textarea is empty', async ({ authedPage: page }) => {
    const btn = page.getByRole('button', { name: /生成应用/ })
    await expect(btn).toBeDisabled()
  })

  test('submit button enables after typing', async ({ authedPage: page }) => {
    await page.getByRole('textbox').fill('我需要一个报销申请系统')
    const btn = page.getByRole('button', { name: /生成应用/ })
    await expect(btn).toBeEnabled()
  })

  test('example buttons fill the textarea', async ({ authedPage: page }) => {
    await page.getByRole('button', { name: '→ 做一个任务管理 App' }).click()
    await expect(page.getByRole('textbox')).toHaveValue('做一个任务管理 App')
  })

  test('center panel shows idle state before generation', async ({ authedPage: page }) => {
    await expect(page.getByText('Agent 团队待命中')).toBeVisible()
  })
})
```

- [ ] **Step 2: 运行测试**

```bash
cd /Users/cookie/project/forge && npx playwright test e2e/layer1/workspace-input.spec.ts --reporter=line
```

期望：6 tests passed。

- [ ] **Step 3: Commit**

```bash
git add e2e/layer1/workspace-input.spec.ts
git commit -m "test(e2e): workspace input phase UI"
```

---

### Task 7: Layer 1 — WorkspacePage PM Review 阶段

**Files:**
- Create: `e2e/layer1/workspace-pm-review.spec.ts`

- [ ] **Step 1: 创建 workspace-pm-review.spec.ts**

```typescript
// e2e/layer1/workspace-pm-review.spec.ts
// Tests: PMReview phase — feature list, toggle, confirm

import { test, expect } from '../fixtures/auth'

// Helper: navigate to /projects/new and submit requirement to trigger pm-review phase
async function submitRequirement(page: import('@playwright/test').Page) {
  await page.goto('/projects/new')
  await page.getByRole('textbox').fill('我需要一个报销申请系统')
  await page.getByRole('button', { name: /生成应用/ }).click()
  // Wait for pm-review phase to appear
  await expect(page.getByText(/我理解你想做/)).toBeVisible({ timeout: 5_000 })
}

test.describe('workspace pm-review phase', () => {
  test('shows AI-amplified feature list after submit', async ({ authedPage: page }) => {
    await submitRequirement(page)
    await expect(page.getByText(/我理解你想做/)).toBeVisible()
    // Feature tiers
    await expect(page.getByText('必需')).toBeVisible()
    await expect(page.getByText('常见')).toBeVisible()
  })

  test('shows confirm button with selected count', async ({ authedPage: page }) => {
    await submitRequirement(page)
    await expect(page.getByRole('button', { name: /确认并生成/ })).toBeVisible()
  })

  test('can toggle a feature off', async ({ authedPage: page }) => {
    await submitRequirement(page)
    // Click first feature row to expand, then uncheck
    const firstFeature = page.getByText('用户认证')
    await firstFeature.click() // expand
    // The checkbox div is next to the feature name
    // Find the checkbox for 用户认证 and click it
    const featureRow = page.locator('div').filter({ hasText: '用户认证' }).first()
    // Click the checkbox (the square div with border)
    await featureRow.locator('div[style*="border-radius: 3px"]').first().click()
    // Confirm button count should decrease
    await expect(page.getByRole('button', { name: /确认并生成 \(3/ })).toBeVisible()
  })

  test('back button returns to input phase', async ({ authedPage: page }) => {
    await submitRequirement(page)
    await page.getByRole('button', { name: '← 返回' }).click()
    await expect(page.getByRole('button', { name: /生成应用/ })).toBeVisible()
  })

  test('confirms and transitions to running phase', async ({ authedPage: page }) => {
    await submitRequirement(page)
    await page.getByRole('button', { name: /确认并生成/ }).click()
    // running phase shows state indicator with pulse dot
    await expect(page.getByText('启动中...')).toBeVisible({ timeout: 5_000 })
  })

  test('preview panel shows 确认需求后开始生成 during pm-review', async ({ authedPage: page }) => {
    await submitRequirement(page)
    await expect(page.getByText('确认需求后开始生成')).toBeVisible()
  })
})
```

- [ ] **Step 2: 运行测试**

```bash
cd /Users/cookie/project/forge && npx playwright test e2e/layer1/workspace-pm-review.spec.ts --reporter=line
```

期望：6 tests passed。

- [ ] **Step 3: Commit**

```bash
git add e2e/layer1/workspace-pm-review.spec.ts
git commit -m "test(e2e): workspace pm-review phase feature list and confirm"
```

---

### Task 8: Layer 1 — WorkspacePage running 阶段

**Files:**
- Create: `e2e/layer1/workspace-running.spec.ts`

- [ ] **Step 1: 创建 workspace-running.spec.ts**

```typescript
// e2e/layer1/workspace-running.spec.ts
// Tests: running phase UI — agent cards, conversation history

import { test, expect } from '../fixtures/auth'

// Helper: get to running phase
async function enterRunningPhase(page: import('@playwright/test').Page) {
  await page.goto('/projects/new')
  await page.getByRole('textbox').fill('我需要一个任务管理 App')
  await page.getByRole('button', { name: /生成应用/ }).click()
  await expect(page.getByText(/我理解你想做/)).toBeVisible({ timeout: 5_000 })
  await page.getByRole('button', { name: /确认并生成/ }).click()
  await expect(page.getByText('启动中...')).toBeVisible({ timeout: 5_000 })
}

test.describe('workspace running phase', () => {
  test('shows all 8 agent cards', async ({ authedPage: page }) => {
    await enterRunningPhase(page)
    // All agent labels should be present
    await expect(page.getByText('PM Agent')).toBeVisible()
    await expect(page.getByText('Architect')).toBeVisible()
    await expect(page.getByText('Schema Agent')).toBeVisible()
    await expect(page.getByText('Logic Agent')).toBeVisible()
    await expect(page.getByText('API Agent')).toBeVisible()
    await expect(page.getByText('UI Agent')).toBeVisible()
    await expect(page.getByText('Page Agent')).toBeVisible()
    await expect(page.getByText('Test Agent')).toBeVisible()
  })

  test('left panel shows conversation history with state indicator', async ({ authedPage: page }) => {
    await enterRunningPhase(page)
    // State indicator dot + text should show
    await expect(page.getByText('启动中...')).toBeVisible()
  })

  test('preview panel shows 应用正在生成中 during running', async ({ authedPage: page }) => {
    await enterRunningPhase(page)
    await expect(page.getByText('应用正在生成中...')).toBeVisible()
  })

  test('progress steps are visible in preview panel', async ({ authedPage: page }) => {
    await enterRunningPhase(page)
    await expect(page.getByText('分析需求')).toBeVisible()
    await expect(page.getByText('规划架构')).toBeVisible()
    await expect(page.getByText('生成代码')).toBeVisible()
    await expect(page.getByText('验证功能')).toBeVisible()
  })
})
```

- [ ] **Step 2: 运行测试**

```bash
cd /Users/cookie/project/forge && npx playwright test e2e/layer1/workspace-running.spec.ts --reporter=line
```

期望：4 tests passed。

- [ ] **Step 3: Commit**

```bash
git add e2e/layer1/workspace-running.spec.ts
git commit -m "test(e2e): workspace running phase agent cards and progress"
```

---

### Task 9: Layer 2 — Agent 进度流（SSE mock）

**Files:**
- Create: `e2e/layer2/agent-flow.spec.ts`

- [ ] **Step 1: 创建 agent-flow.spec.ts**

```typescript
// e2e/layer2/agent-flow.spec.ts
// @slow
// Tests: agent progress cards update when SSE events arrive

import { test, expect } from '../fixtures/auth'

const PROJECT_ID = 'test-proj-sse'

// Build a fake SSE response body from an array of events
function buildSSEBody(events: object[]): string {
  return events.map(e => `event: agent_event\ndata: ${JSON.stringify(e)}\n\n`).join('')
    + `event: done\ndata: {"previewUrl":"https://test.e2b.dev/preview"}\n\n`
}

// Helper: go to running phase with SSE mock in place for PROJECT_ID
async function enterRunningWithSSE(
  page: import('@playwright/test').Page,
  events: object[],
) {
  // Mock the SSE stream endpoint
  await page.route(`**/api/v1/projects/${PROJECT_ID}/stream`, route => {
    route.fulfill({
      status: 200,
      headers: { 'Content-Type': 'text/event-stream', 'Cache-Control': 'no-cache' },
      body: buildSSEBody(events),
    })
  })

  // Navigate directly to the project workspace (simulates returning to an existing project)
  // The WorkspacePage useEffect will call startGeneration(PROJECT_ID) and open SSE
  await page.goto(`/projects/${PROJECT_ID}`)
}

test.describe('agent flow via SSE', () => {
  test('agent card transitions to running when agent_start event arrives', async ({ authedPage: page }) => {
    await enterRunningWithSSE(page, [
      { type: 'agent_start', agent: 'pm', message: 'Analyzing requirements...' },
    ])
    await expect(page.getByText('PM Agent')).toBeVisible()
    // PM Agent card should show running status (pulse dot) or current action
    await expect(page.getByText('Analyzing requirements...')).toBeVisible({ timeout: 5_000 })
  })

  test('agent card shows current action on agent_thinking event', async ({ authedPage: page }) => {
    await enterRunningWithSSE(page, [
      { type: 'agent_start', agent: 'architect', message: 'Starting...' },
      { type: 'agent_thinking', agent: 'architect', content: 'Designing the schema...' },
    ])
    await expect(page.getByText('Designing the schema...')).toBeVisible({ timeout: 5_000 })
  })

  test('agent card shows file write', async ({ authedPage: page }) => {
    await enterRunningWithSSE(page, [
      { type: 'agent_start', agent: 'schema', message: 'Starting...' },
      { type: 'agent_file_write', agent: 'schema', file: 'infra/sqlc/queries/project.sql', action: 'create' },
    ])
    await expect(page.getByText('project.sql')).toBeVisible({ timeout: 5_000 })
  })

  test('agent card shows done status on agent_done event', async ({ authedPage: page }) => {
    await enterRunningWithSSE(page, [
      { type: 'agent_start', agent: 'pm', message: 'Starting...' },
      { type: 'agent_done', agent: 'pm', summary: 'Requirements analyzed' },
    ])
    // Done state shows ✓ in card
    const pmCard = page.locator('div').filter({ hasText: 'PM Agent' }).first()
    await expect(pmCard.getByText('✓')).toBeVisible({ timeout: 5_000 })
  })

  test('agent card shows error status on agent_error event', async ({ authedPage: page }) => {
    await enterRunningWithSSE(page, [
      { type: 'agent_start', agent: 'logic', message: 'Starting...' },
      { type: 'agent_error', agent: 'logic', error: 'Compilation failed' },
    ])
    await expect(page.getByText('Compilation failed')).toBeVisible({ timeout: 5_000 })
  })

  test('orchestrator state bar updates on state_change event', async ({ authedPage: page }) => {
    await enterRunningWithSSE(page, [
      { type: 'state_change', state: 'building' },
    ])
    await expect(page.getByText('生成代码')).toBeVisible({ timeout: 5_000 })
  })

  test('AI thinking log appears and is expandable', async ({ authedPage: page }) => {
    await enterRunningWithSSE(page, [
      { type: 'agent_thinking', agent: 'architect', content: 'Thinking about architecture...' },
    ])
    // Log drawer toggle button appears
    await expect(page.getByText(/AI 思考日志/)).toBeVisible({ timeout: 5_000 })
    // Click to expand
    await page.getByText(/AI 思考日志/).click()
    await expect(page.getByText('Thinking about architecture...')).toBeVisible()
  })
})
```

- [ ] **Step 2: 运行测试**

```bash
cd /Users/cookie/project/forge && npx playwright test e2e/layer2/agent-flow.spec.ts --reporter=line
```

期望：7 tests passed。

- [ ] **Step 3: Commit**

```bash
git add e2e/layer2/agent-flow.spec.ts
git commit -m "test(e2e): agent flow SSE events update agent cards"
```

---

### Task 10: Layer 2 — 预览面板（SSE mock）

**Files:**
- Create: `e2e/layer2/preview-panel.spec.ts`

- [ ] **Step 1: 创建 preview-panel.spec.ts**

```typescript
// e2e/layer2/preview-panel.spec.ts
// @slow
// Tests: preview panel iframe appears when SSE done event fires

import { test, expect } from '../fixtures/auth'

const PROJECT_ID = 'test-proj-preview'
const PREVIEW_URL = 'https://test.e2b.dev/preview-app'

async function mockSSEWithDone(page: import('@playwright/test').Page) {
  await page.route(`**/api/v1/projects/${PROJECT_ID}/stream`, route => {
    route.fulfill({
      status: 200,
      headers: { 'Content-Type': 'text/event-stream', 'Cache-Control': 'no-cache' },
      body: [
        `event: agent_event\ndata: ${JSON.stringify({ type: 'state_change', state: 'building' })}\n\n`,
        `event: done\ndata: ${JSON.stringify({ previewUrl: PREVIEW_URL })}\n\n`,
      ].join(''),
    })
  })
  await page.goto(`/projects/${PROJECT_ID}`)
}

test.describe('preview panel', () => {
  test('shows placeholder when no previewUrl', async ({ authedPage: page }) => {
    await page.goto('/projects/new')
    await expect(page.getByText('输入需求后预览将出现在这里')).toBeVisible()
  })

  test('iframe appears when SSE done event fires with previewUrl', async ({ authedPage: page }) => {
    await mockSSEWithDone(page)
    // After done event, iframe should appear with the previewUrl
    const iframe = page.locator('iframe[title="App Preview"]')
    await expect(iframe).toBeVisible({ timeout: 10_000 })
    await expect(iframe).toHaveAttribute('src', PREVIEW_URL)
  })

  test('URL bar shows previewUrl after done', async ({ authedPage: page }) => {
    await mockSSEWithDone(page)
    await page.locator('iframe[title="App Preview"]').waitFor({ timeout: 10_000 })
    // URL bar div should contain the preview URL
    await expect(page.getByText(PREVIEW_URL)).toBeVisible()
  })

  test('refresh button reloads the iframe', async ({ authedPage: page }) => {
    await mockSSEWithDone(page)
    await page.locator('iframe[title="App Preview"]').waitFor({ timeout: 10_000 })
    // Refresh button appears
    const refreshBtn = page.getByTitle('刷新预览')
    await expect(refreshBtn).toBeVisible()
    await refreshBtn.click()
    // iframe still present after refresh
    await expect(page.locator('iframe[title="App Preview"]')).toBeVisible()
  })

  test('open in new tab button appears after done', async ({ authedPage: page }) => {
    await mockSSEWithDone(page)
    await page.locator('iframe[title="App Preview"]').waitFor({ timeout: 10_000 })
    await expect(page.getByTitle('在新标签页打开')).toBeVisible()
  })
})
```

- [ ] **Step 2: 运行测试**

```bash
cd /Users/cookie/project/forge && npx playwright test e2e/layer2/preview-panel.spec.ts --reporter=line
```

期望：5 tests passed。

- [ ] **Step 3: Commit**

```bash
git add e2e/layer2/preview-panel.spec.ts
git commit -m "test(e2e): preview panel iframe and controls"
```

---

### Task 11: Layer 2 — Waiting 阶段（SSE mock）

**Files:**
- Create: `e2e/layer2/waiting-state.spec.ts`

- [ ] **Step 1: 创建 waiting-state.spec.ts**

```typescript
// e2e/layer2/waiting-state.spec.ts
// @slow
// Tests: waiting phase — user input box appears, message displayed

import { test, expect } from '../fixtures/auth'

const PROJECT_ID = 'test-proj-waiting'

async function mockSSEWithWaiting(page: import('@playwright/test').Page) {
  await page.route(`**/api/v1/projects/${PROJECT_ID}/stream`, route => {
    route.fulfill({
      status: 200,
      headers: { 'Content-Type': 'text/event-stream', 'Cache-Control': 'no-cache' },
      body: [
        `event: agent_event\ndata: ${JSON.stringify({ type: 'state_change', state: 'building' })}\n\n`,
        `event: agent_event\ndata: ${JSON.stringify({ type: 'waiting', reason: '需要你确认数据库字段设计' })}\n\n`,
      ].join(''),
    })
  })
  await page.goto(`/projects/${PROJECT_ID}`)
}

test.describe('waiting state', () => {
  test('shows waiting reason message', async ({ authedPage: page }) => {
    await mockSSEWithWaiting(page)
    await expect(page.getByText('AI 卡住了，需要你的帮助')).toBeVisible({ timeout: 10_000 })
    await expect(page.getByText('需要你确认数据库字段设计')).toBeVisible()
  })

  test('iteration input box appears in waiting phase', async ({ authedPage: page }) => {
    await mockSSEWithWaiting(page)
    await page.getByText('AI 卡住了，需要你的帮助').waitFor({ timeout: 10_000 })
    await expect(page.getByPlaceholder('告诉 AI 怎么解决...')).toBeVisible()
    await expect(page.getByRole('button', { name: '发送' })).toBeVisible()
  })

  test('iteration input can be typed into', async ({ authedPage: page }) => {
    await mockSSEWithWaiting(page)
    await page.getByText('AI 卡住了，需要你的帮助').waitFor({ timeout: 10_000 })
    const input = page.getByPlaceholder('告诉 AI 怎么解决...')
    await input.fill('保持原来的字段设计，继续')
    await expect(input).toHaveValue('保持原来的字段设计，继续')
  })

  test('send button disabled when input is empty', async ({ authedPage: page }) => {
    await mockSSEWithWaiting(page)
    await page.getByText('AI 卡住了，需要你的帮助').waitFor({ timeout: 10_000 })
    const sendBtn = page.getByRole('button', { name: '发送' })
    await expect(sendBtn).toBeDisabled()
  })
})
```

- [ ] **Step 2: 运行测试**

```bash
cd /Users/cookie/project/forge && npx playwright test e2e/layer2/waiting-state.spec.ts --reporter=line
```

期望：4 tests passed。

- [ ] **Step 3: Commit**

```bash
git add e2e/layer2/waiting-state.spec.ts
git commit -m "test(e2e): waiting state user intervention flow"
```

---

### Task 12: 全量运行 + CI script

**Files:**
- Modify: `package.json`（根目录）— 确认 scripts 齐全
- Create: `e2e/README.md`

- [ ] **Step 1: 全量跑 Layer 1**

```bash
cd /Users/cookie/project/forge && npx playwright test e2e/layer1 --reporter=line
```

期望：23 tests passed（routing:4 + login:3 + projects:9 + workspace-input:6 + workspace-pm-review:6 + workspace-running:4）。

如果有失败，根据错误修复对应 spec 文件（不改业务代码）。

- [ ] **Step 2: 全量跑 Layer 2**

```bash
cd /Users/cookie/project/forge && npx playwright test e2e/layer2 --reporter=line
```

期望：16 tests passed（agent-flow:7 + preview-panel:5 + waiting-state:4）。

- [ ] **Step 3: 创建 e2e/README.md**

```markdown
# E2E Tests

Playwright E2E tests for Forge frontend.

## Prerequisites

Both services must be running before running tests:

```bash
# Terminal 1
cd apps/api && go run ./cmd/server

# Terminal 2
cd apps/web && pnpm dev
```

## Running Tests

```bash
# Layer 1 only (fast, no SSE mock required) — use for quick validation
npm run e2e:layer1

# Layer 2 only (SSE mock, agent flow)
npm run e2e:layer2

# All tests
npm run e2e

# With UI
npx playwright test --ui
```

## Test Structure

```
e2e/
├── fixtures/
│   └── auth.ts              # Authenticated page fixture (performs skip login)
├── layer1/                  # No SSE — stable, fast
│   ├── routing.spec.ts      # Route redirects and auth guard
│   ├── login.spec.ts        # Skip login flow
│   ├── projects.spec.ts     # Project list, empty state, cards
│   ├── workspace-input.spec.ts      # WorkspacePage input phase
│   ├── workspace-pm-review.spec.ts  # PM review feature selection
│   └── workspace-running.spec.ts    # Running phase agent cards
└── layer2/                  # SSE mocked via page.route()
    ├── agent-flow.spec.ts   # Agent card updates from SSE events
    ├── preview-panel.spec.ts # Preview iframe on done event
    └── waiting-state.spec.ts # Waiting phase user input
```
```

- [ ] **Step 4: 最终 commit**

```bash
cd /Users/cookie/project/forge
git add e2e/README.md package.json
git commit -m "test(e2e): complete E2E test suite — 39 tests across layer1 and layer2"
```
